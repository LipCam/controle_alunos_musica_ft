name: controle_alunos_musica_ft
description: Controle Alunos Música FT.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: 2.0.3+1
  mobx: 2.0.7+5
  flutter_mobx: 2.0.6+1
  url_launcher: 6.1.5
  fluttertoast: 8.0.9
  intl: ^0.17.0
  path_provider: ^2.0.11
  open_file: ^3.2.1

  cupertino_icons: ^1.0.2
  pdf: ^3.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 2.2.0
  mobx_codegen: 2.0.7

  
  flutter_lints: ^2.0.0 #comente para remover as linhas azuis chatas da P@rr@


flutter:

  uses-material-design: true

  assets:
    - assets/images/